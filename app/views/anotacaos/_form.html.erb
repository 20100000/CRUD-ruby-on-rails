<%= form_for(@anotacao) do |f|  %>
  <% if @anotacao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@anotacao.errors.count, "error") %> prohibited this anotacao from being saved:</h2>

      <ul>
      <% @anotacao.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body, class: 'form-control' %>
  </div>
    <div class="field ">
      <%= f.label "Amigo" %><br>
      <%= f.collection_select :amigo_id, @amigoss, :id ,:name, { include_blank: true }, { class: "form-control " } %><br/>
    </div>
  <div class="actions pull-right">
    <%= f.submit class: "btn btn-success"%>
  </div>
<% end %>
